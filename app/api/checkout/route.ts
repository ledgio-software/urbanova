// Phase 3 — full implementation in Phase 3
import { NextResponse } from 'next/server'

export async function POST() {
  return NextResponse.json(
    { error: 'Checkout endpoint not yet implemented — Phase 3' },
    { status: 501 }
  )
}
